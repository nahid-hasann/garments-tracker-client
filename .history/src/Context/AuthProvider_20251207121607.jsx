import React, { useState } from 'react';

const AuthProvider = () => {

    const [user, SetUser] = useState(null);
    const [loading, set]

    return (
        <div>
            
        </div>
    );
};

export default AuthProvider;