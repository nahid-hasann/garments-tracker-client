import { useQuery } from '@tanstack/react-query';
import React from 'react';

const MyOrders = () => {

    const {} = useQuery()

    return (
        <div>
            
        </div>
    );
};

export default MyOrders;