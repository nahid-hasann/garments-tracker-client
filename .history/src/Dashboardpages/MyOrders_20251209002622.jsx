import { useQuery } from '@tanstack/react-query';
import React from 'react';

const MyOrders = () => {

    const {} = useQuery({
        queryKey: 
    })

    return (
        <div>
            
        </div>
    );
};

export default MyOrders;